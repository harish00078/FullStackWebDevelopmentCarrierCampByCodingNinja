// here we are learning about the modules:

// here we are using the function that we have created in the (Operations) file using (modules-export) function:

// for using that function's here we have import them:
const Operations = require('./Operation');

console.log(Operations.add(2,5))
console.log(Operations.multiply(2,5));
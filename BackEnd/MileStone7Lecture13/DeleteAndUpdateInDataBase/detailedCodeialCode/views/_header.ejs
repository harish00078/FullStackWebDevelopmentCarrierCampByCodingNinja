<header id="layout-header">
    <nav>
        <div class="mini-logo">
            <p>
                <a href="/">codeial</a>


            </p>
        </div>

        <!-- here we show some (data) of the (user) on the (header) of the  webpages: -->

        <div class="auth-links">
            <ul>

                <!-- here we are doing that:if we have (locals.user):it means that one of the (user) has successfully (signed-in) in the (website):-->
                <!-- and then  we have to (show) some of its data on the (Header) of the (webpages): -->

                <% if (locals.user){ %>


                    <!-- if we have locals (user):then we have show its (name) on the (header) of the (webpages): -->
                    <!-- <li><%= user.name%></li> -->

                    <!-- IMP = instead of simply (showing) the (name) of the (user):what we can do is that we can connect the (name) of (user):with the  (login) user's (profile-page):
        => so after (signing-in) on the (website):when ever (user) click on its (name):that is show on the (header) of (home-page):
        => that will  get him to its (profile-page):were he  had its all data related to its (profile) on the  (website): -->

                    <li>
                        <a href="/users/profile">
                            <%= user.name %>
                        </a>
                    </li>


                    <!-- we also gave the (sign-out) link in (header) of the (webpages): -->
                    <li>
                        <a href="/users/sign-out">sign Out</a>
                    </li>



                    <!-- if does not have (locals.user):it means that none off the (user) has been successfully able to (login) on the webpage:
        and then we have to show something else on the (header) of the webpages -->
                    <% }else{ %>

                        <!-- if we does not have the (locals.user):means that if (user) does not (successfuly) able to (signed-in): -->
                        <li>
                            <!-- then we can gave him the link of the (singed-in) webpage: -->
                            <a href="/users/sign-in">Sign-In</a>
                        </li>

                        <!-- and we can also gave him the link of the (sign-up) webpage: if he want to (sign-up) on the (website) : -->
                        <li>
                            <a href="/users/sign-up">Sign-Up</a>
                        </li>


                        <% } %>
            </ul>

        </div>

    </nav>


</header>